[pytest]
testpaths = tests
pythonpath = .
addopts = -q --import-mode=prepend -m "not e2e"
markers =
    live: lance un vrai serveur uvicorn en sous-processus (test d'intégration)
    e2e: test navigateur Playwright (lent, Chromium) — hors run par défaut ; lancer avec `pytest -m e2e`
