# Dépendances de développement / tests
-r requirements.txt
pytest>=8.0
pytest-cov>=4.0
httpx>=0.27          # requis par fastapi.testclient + tests d'intégration
# Pour exécuter les tests de segmentation, Kumiko doit être installé :
#   git clone https://github.com/njean42/kumiko.git lib/kumiko
#   pip install opencv-python-headless numpy requests
